language: node_js
node_js:
- "8.2.1"

install:
- npm install newman

before_script:
- node --version
- npm --version
- newman --version

script:
- newman run test/API Automation.postman_collection.json -g test/My Workspace.postman_globals.json
